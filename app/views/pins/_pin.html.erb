<div class="box">
	<%= image_tag pin.image(:thumb) %>
	<%= pin.description %>
	<%= link_to 'Show', pin %>
	<% if current_user == pin.user %>
		<%= link_to 'Edit', edit_pin_path(pin) %>
		<%= link_to 'Destroy', pin, confirm: 'Are you sure?', method: :delete %>
	<% end %>
</div>	

    